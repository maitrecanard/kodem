/.phpunit.cache
/bootstrap/ssr
/composer.phar
/database/database.sqlite
/node_modules
# public/build est volontairement versionné : le serveur de production ne
# compile pas les assets (pas de Node), il se contente d'un git pull.
# Après chaque `npm run build`, committer les bundles régénérés.
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed

# Sources de travail déposées à la racine (images non destinées au dépôt).
# Les visuels publiés vivent dans public/images/, convertis en WebP.
/*.jpg
/*.jpeg
/*.png
